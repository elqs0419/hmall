server:
  port: 8080
hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall
    password: hmall123
    tokenTTL: 30m
  auth:
    exclude-paths:
      - /users/login
      - /search/**
      - /items/**
      - /hi
